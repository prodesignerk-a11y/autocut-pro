{
  "name": "@autocut/shared",
  "version": "1.0.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format cjs --dts --out-dir dist"
  },
  "devDependencies": {
    "tsup": "^8.1.0",
    "typescript": "^5.4.0"
  }
}
